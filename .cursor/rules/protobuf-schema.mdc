---
description: schema/ 下 Protobuf 为 Dec 配置与资产声明的 schema 真相源
globs: schema/**/*.proto, internal/types/**/*.go, internal/config/**/*.go, internal/app/**/*.go
alwaysApply: false
---

# Protobuf Schema 真相源

`schema/` 目录（`dec/v1`、`secrets/v1`）是 Dec **配置与资产声明层**的 schema 真相源；运行时 wire format 仍是 YAML / JSON，不引入 `.pb` 二进制配置文件。

## 适用 proto 的字段

- 顶层 P（`<p>/dec.yaml`）、`ProjectConfig`、`GlobalConfig`、`VarsConfig`
- `requires` 与四象限资产视图
- 本地 `ProjectConfig.project_name` / `GlobalConfig.enabled_projects`

## 不适用 proto（保持原格式）

- `SKILL.md`、`.mdc` 规则正文
- MCP JSON / TOML 片段正文
- Bitwarden Secure Note / SSH 私钥明文
- 资产目录布局（由代码扫描实现，不进 proto）

## 实现约定

- 修改 schema 时先改 `schema/**/*.proto`，再同步 `internal/types/` 手写 struct 与 adapter（生成物见 `schema/gen/go/`）。
- P 名强制小写 kebab-case；资产正文不进 proto。
- Git 使用 `<p>/{public,private}/{user,project}`；四支都只能保存非敏感资产。
- `requires` 只直接引用目标 P 的 `public/project`，不递归，private 永不被引用。
- Bitwarden 后续只保存 `<p>/private/{user,project}` 的敏感正文，不建立 public folder。

## 示例

```yaml
# .dec/config.yaml — wire format 仍是 YAML
version: v2
project_name: my-app
```

```yaml
# my-app/dec.yaml — 顶层 P 声明
name: my-app
requires:
  - shared-tools
```

```protobuf
// schema/dec/v1/projects.proto — vault project 真相源
message Project {
  repeated string requires = 4;
}
```
