# VK-42 阶段 3A：TUI Assets 页接入资产选择与保存

- Vikunja 项目：`Dec`（ID `7`）
- Vikunja 任务：`42`
- 任务链接：`http://vikunja.firoyang.com/tasks/42`
- 计划目录：`Documents/tasks/`
- 命名约定：`VK-<id>.md`

## 本轮停止条件

- TUI 的 `Assets` 页可展示项目资产列表，并支持最小可用的导航与启用切换
- 用户可在 TUI 内保存启用状态到 `.dec/config.yaml`
- 保存后首页概览和 `Assets` 页状态同步刷新
- 保留 `dec config init` 现有 CLI 回退路径，不破坏已有行为
- 补齐资产加载、切换、保存和 TUI 交互的关键测试

## 实施步骤

1. 梳理当前 `pkg/app/project.go`、`pkg/app/overview.go` 和 TUI `Assets` 页占位实现，明确资产加载/保存边界。
2. 在 `pkg/app` 新增资产选择用例，封装 available/enabled 加载、启用切换结果保存和 vars 模板确保逻辑。
3. 扩展 `internal/tui/model.go`，让 `Assets` 页展示资产列表并支持光标移动、启用切换、保存、刷新。
4. 保存成功后刷新 overview 与日志，让首页和状态栏看到最新 enabled 数量。
5. 补充 `pkg/app` 与 `internal/tui` 测试，覆盖加载、切换、保存、刷新等关键路径。

## 测试范围

- `go test ./...`

## 涉及模块

- `pkg/app/*`
- `internal/tui/*`
- `cmd/config.go`（仅在需要复用用例层时调整）
- `Documents/ARCHITECTURE.md`
