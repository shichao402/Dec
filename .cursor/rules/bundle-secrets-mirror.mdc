---
description: Dec bundle 与 Bitwarden secrets bundle 同构原则、.secrets SyncTarget、零重叠 invariant、TUI pull 流程
globs: pkg/app/**/*.go, internal/tui/**/*.go, schema/secrets/**/*.proto, schema/dec/**/*.proto, Documents/**
alwaysApply: false
---

# Bundle ↔ Secrets Bundle 同构

Dec Git Vault 与 Bitwarden 以 **bundle** 为对称组织单位；**project**（`projects/<name>.yaml`）声明启用哪些 bundle。Bitwarden 侧称为 **secrets bundle**（folder）。Git Vault 顶层仅含 `projects/` 与 `bundles/`。

决策依据：[0002-secrets-synctarget-root.md](../../Documents/decisions/0002-secrets-synctarget-root.md)（取代 0001）。

## 存储根分离（核心）

| 平面 | 存储根 | 内容 |
|------|--------|------|
| Dec（非敏感） | 项目 **`.dec/`** | cache、config 等；渲染到 `.cursor/` 等 IDE 目录 |
| Bitwarden（敏感） | 项目 **`.secrets/`** 或 **`~/.ssh/`** | Secure Note 在 `.secrets/project/` 或 `.secrets/bundles/<name>/`；SSH Key 在 `~/.ssh/` |

- bundle 内目录结构 **同构**（`skills/`、`rules/`、`mcp/` 等），但 **存储根完全不同**。
- 默认 **folder 约定**：project ↔ 裸名（`Dec` / `lyra`）；bundle ↔ `bundle/<name>`（如 `bundle/vikunja`）。可用 `secrets_bundle` 显式覆盖。
- **SyncTarget**：`folder` ↔ `LocalRoot`（`.secrets/project` 或 `.secrets/bundles/<name>`）。
- **Secure Note 名称** = 相对 `LocalRoot` 的路径（如 `env/vikunja.env`）。
- **SSH Key**：Pull → `~/.ssh/dec_<bundle>_<name>` + Dec 管理 `~/.ssh/config` 区块。

## 落地前校验（硬约束）

所有校验必须在**写任何文件之前**完成。落点在 `pkg/secrets/landing.go` 的 `ValidateLandingPaths`。

- **非法路径**：绝对路径、`~` 前缀、`..` 逃逸、盘符 → 拒绝。
- **`.secrets/` 边界**：落地路径必须在 `.secrets/` 下；整树须被 `.gitignore` 忽略。
- **`.dec/` 重叠**：`.dec/` 树与 `.secrets/` 树**禁止相交**。
- **跨 folder 撞车**：汇总后映射到同一项目路径 → 拒绝。
- **符号链接逃逸**、**git 跟踪** → 拒绝。

## Push 语义（硬约束）

- push **递归扫描** `SyncTarget.LocalRoot`（create/update）。
- push **绝不删远端 note**。本地缺文件记入 `MissingLocal`。
- 删除只走 Remote 页的逐条确认。
- 新增 secret 走 TUI Project 页 `A`（`app.AddSecretToTarget`）。
- pull **不做清理**。

## 环境变量与 MCP

- 环境变量只认 **`env/*.env`**（dotenv）。
- 第三方 MCP 经 hidden **`dec exec --bundle <name>`** 注入；**不要**用 mise / 外部启动器。
- **不要** 用 `{{TOKEN}}` 占位符替换作为 secrets 注入主路径。

## Pull 流程（TUI-first）

0. 解析 `project_name` → vault `projects/<name>.yaml` → bundles 列表
1. 拉 Dec Git bundle → `.dec/cache/<bundle>/`
2. **自动**从各 SyncTarget 的 folder **取回** Secure Note（先不写盘）
3. 汇总全部落地路径，一次校验
4. 落地：Secure Note → **`.secrets/`**；SSH Key → **`~/.ssh/`**
5. 从 cache 渲染安装 IDE + 非敏感 vars 占位符替换

用户在 TUI **Run** 页一次 pull 完成上述步骤；**不要**新增独立的 secrets pull CLI 子命令。

## 参考

- ADR：[Documents/decisions/0002-secrets-synctarget-root.md](../../Documents/decisions/0002-secrets-synctarget-root.md)
- 详细设计：`Documents/BUNDLE-SECRETS-MODEL.md`
- Schema：`schema/secrets/v1/config.proto`（`BundleBinding`）

**无本地同步状态文件**：不要重新引入 `state.json` / `SyncStateFile`。
