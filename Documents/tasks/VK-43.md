# VK-43 阶段 4A：TUI Run 页接入 pull 执行流

- Vikunja 项目：`Dec`（ID `7`）
- Vikunja 任务：`43`
- 任务链接：`http://vikunja.firoyang.com/tasks/43`
- 计划目录：`Documents/tasks/`
- 命名约定：`VK-<id>.md`

## 本轮停止条件

- `pkg/app` 提供可复用的 `pull` 用例层，输出结构化事件而不是直接打印
- `dec pull` CLI 改为复用该用例层，现有核心行为保持兼容
- TUI `Run` 页可触发一次 `pull`，展示执行状态/日志，并在成功后刷新首页与资产状态
- 补齐 `pull` 用例层与 TUI Run 页关键测试
- `Documents/ARCHITECTURE.md` 同步 `operations` 与 Run 页的新边界

## 实施步骤

1. 抽离 `cmd/pull.go` 里的核心编排到 `pkg/app/operations.go`，明确输入、结果和事件模型。
2. 保持 `cmd/pull.go` 作为 CLI 适配层，只负责渲染警告、日志和最终摘要。
3. 扩展 `internal/tui/model.go`，让 `Run` 页支持触发 `pull`、查看阶段状态并在完成后刷新 overview/assets。
4. 为 `pkg/app` 和 `internal/tui` 增加测试，覆盖成功路径、空配置/错误处理和状态迁移。
5. 更新 `Documents/ARCHITECTURE.md`，说明 Run 页已开始承接执行操作，当前先覆盖 `pull`。

## 测试范围

- `go test ./...`

## 涉及模块

- `pkg/app/operations.go`
- `pkg/app/operations_test.go`
- `cmd/pull.go`
- `internal/tui/model.go`
- `internal/tui/model_test.go`
- `Documents/ARCHITECTURE.md`
