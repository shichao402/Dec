name: Sync Registry

on:
  # 定时同步（每小时）
  schedule:
    - cron: '0 * * * *'
  
  # 手动触发
  workflow_dispatch:
    inputs:
      repository:
        description: '指定仓库 URL（留空则同步全部）'
        required: false
        type: string
  
  # Issue 触发（单包同步）
  issues:
    types: [opened]

  # Repository dispatch 触发（包发布后自动同步）
  repository_dispatch:
    types: [package-released]

jobs:
  # 单包同步（通过 issue 触发）
  sync-single:
    # 使用 label 识别 sync issue（更可靠）
    if: github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'pack-sync')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse issue
        id: parse
        run: |
          # 优先从 payload 文件读取（如果使用 github-issue 工具创建）
          # 否则从 issue body 或 title 解析
          BODY="${{ github.event.issue.body }}"
          TITLE="${{ github.event.issue.title }}"
          
          # 尝试从 body 中提取 Gist URL（github-issue 工具会将 payload 存储在 Gist）
          GIST_URL=$(echo "$BODY" | grep -oP 'https://gist\.github\.com/[^\s\)]+' | head -1 || echo "")
          
          if [ -n "$GIST_URL" ]; then
            echo "📦 从 Gist 读取 payload..."
            # 从 Gist URL 提取 Gist ID
            GIST_ID=$(echo "$GIST_URL" | sed 's|https://gist.github.com/||' | awk '{print $1}')
            # 下载 payload JSON
            PAYLOAD_URL="https://gist.githubusercontent.com/$GIST_ID/raw"
            if curl -sfL "$PAYLOAD_URL" -o /tmp/payload.json; then
              REPO=$(jq -r '.repository // empty' /tmp/payload.json)
              if [ -n "$REPO" ] && [ "$REPO" != "null" ]; then
                echo "✅ 从 payload 读取到 repository: $REPO"
                echo "repository=$REPO" >> $GITHUB_OUTPUT
                echo "📦 Repository: $REPO"
                exit 0
              fi
            fi
          fi
          
          # 回退方案：从 body 或 title 解析
          echo "📦 从 issue body/title 解析 repository..."
          
          # 尝试从 body 提取 repository（格式：repository: https://...）
          REPO=$(echo "$BODY" | grep -oP 'repository:\s*\K(https://github.com/[^\s\)]+)' | head -1 || echo "")
          
          # 如果 body 中没有，尝试从 title 解析
          if [ -z "$REPO" ]; then
            # 移除 [sync] 前缀并清理空格
            REPO=$(echo "$TITLE" | sed 's/^\[sync\]\s*//' | xargs)
            
            # 移除版本号（匹配 v1.2.3 或 1.2.3 格式，可能前面有空格）
            # 使用 awk 提取第一个字段（在空格或版本号之前）
            if [[ "$REPO" =~ ^https://github.com/ ]]; then
              # 完整 URL：提取到版本号之前
              REPO=$(echo "$REPO" | awk '{print $1}' | sed 's/\s*v[0-9].*$//' | sed 's/\s*[0-9]\+\.[0-9]\+\.[0-9].*$//')
            else
              # owner/repo 格式：提取第一个字段（在空格之前）
              REPO=$(echo "$REPO" | awk '{print $1}')
              # 补全为完整 URL
              REPO="https://github.com/$REPO"
            fi
          fi
          
          # 规范化：移除 .git 后缀和尾部斜杠
          REPO=$(echo "$REPO" | sed 's/\.git$//' | sed 's/\/$//')
          
          if [ -z "$REPO" ]; then
            echo "error=无法从 issue 中提取 repository URL" >> $GITHUB_OUTPUT
            exit 1
          fi
          
          echo "repository=$REPO" >> $GITHUB_OUTPUT
          echo "📦 Repository: $REPO"

      - name: Sync package
        id: sync
        run: |
          REPO="${{ steps.parse.outputs.repository }}"
          
          # 检查仓库是否在 registry 中
          if ! jq -e ".packages[] | select(.repository == \"$REPO\")" config/registry.json > /dev/null 2>&1; then
            echo "error=仓库未注册，请先使用 [auto-register] 注册" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # 下载最新的 package.json
          PKG_URL="$REPO/releases/latest/download/package.json"
          if ! curl -sfL "$PKG_URL" -o /tmp/package.json; then
            echo "error=无法下载 package.json" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # 提取信息
          NAME=$(jq -r '.name' /tmp/package.json)
          VERSION=$(jq -r '.version' /tmp/package.json)
          
          # 更新 registry 中对应条目
          jq --arg repo "$REPO" \
             --argjson pkg "$(cat /tmp/package.json)" \
             '(.packages[] | select(.repository == $repo)) |= {
               repository: $repo,
               name: $pkg.name,
               version: $pkg.version,
               description: ($pkg.description // ""),
               author: ($pkg.author // ""),
               dist: $pkg.dist
             }' config/registry.json > /tmp/registry.json
          mv /tmp/registry.json config/registry.json
          
          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "synced=true" >> $GITHUB_OUTPUT
          echo "✅ 已同步: $NAME@$VERSION"

      - name: Commit and push
        if: steps.sync.outputs.synced == 'true'
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add config/registry.json
          
          # 检查是否有变化
          if git diff --cached --quiet; then
            echo "ℹ️ 无变化，跳过提交"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            git commit -m "chore: sync ${{ steps.sync.outputs.name }}@${{ steps.sync.outputs.version }}"
            git push
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Update registry release
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "📦 更新 registry release..."
          if gh release view registry &>/dev/null; then
            gh release upload registry config/registry.json --clobber
          else
            gh release create registry \
              --title "Package Registry" \
              --notes "This release contains the package registry for Dec." \
              config/registry.json
          fi
          echo "✅ Registry release 更新完成"

      - name: Comment result
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ steps.sync.outputs.synced }}" == "true" ]; then
            gh issue comment ${{ github.event.issue.number }} --body "✅ **同步成功！**

            - **包名**: ${{ steps.sync.outputs.name }}
            - **版本**: ${{ steps.sync.outputs.version }}

            用户运行 \`dec update\` 即可获取最新版本。"
          elif [ -n "${{ steps.sync.outputs.error }}" ]; then
            gh issue comment ${{ github.event.issue.number }} --body "❌ **同步失败**

            ${{ steps.sync.outputs.error }}"
          fi
          
          gh issue close ${{ github.event.issue.number }}

  # 全量同步（定时或手动触发）或单包同步（repository_dispatch）
  sync-all:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync all packages
        run: |
          echo "🔄 开始同步所有包..."
          
          # 如果指定了单个仓库（来自 workflow_dispatch 或 repository_dispatch）
          SINGLE_REPO="${{ github.event.inputs.repository }}"
          
          # repository_dispatch 触发时从 client_payload 获取仓库
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            SINGLE_REPO="${{ github.event.client_payload.repository }}"
            echo "📦 触发来源: repository_dispatch"
            echo "📦 同步仓库: $SINGLE_REPO"
          fi
          
          # 获取所有仓库
          REPOS=$(jq -r '.packages[].repository' config/registry.json)
          
          UPDATED=0
          FAILED=0
          
          for REPO in $REPOS; do
            # 如果指定了单个仓库，跳过其他
            if [ -n "$SINGLE_REPO" ] && [ "$REPO" != "$SINGLE_REPO" ]; then
              continue
            fi
            
            echo ""
            echo "📦 同步: $REPO"
            
            # 下载 package.json
            PKG_URL="$REPO/releases/latest/download/package.json"
            if ! curl -sfL "$PKG_URL" -o /tmp/package.json 2>/dev/null; then
              echo "  ⚠️ 跳过（无法下载 package.json）"
              FAILED=$((FAILED + 1))
              continue
            fi
            
            # 验证 JSON
            if ! jq -e '.name' /tmp/package.json > /dev/null 2>&1; then
              echo "  ⚠️ 跳过（package.json 格式错误）"
              FAILED=$((FAILED + 1))
              continue
            fi
            
            NAME=$(jq -r '.name' /tmp/package.json)
            VERSION=$(jq -r '.version' /tmp/package.json)
            
            # 更新 registry
            jq --arg repo "$REPO" \
               --argjson pkg "$(cat /tmp/package.json)" \
               '(.packages[] | select(.repository == $repo)) |= {
                 repository: $repo,
                 name: $pkg.name,
                 version: $pkg.version,
                 description: ($pkg.description // ""),
                 author: ($pkg.author // ""),
                 dist: $pkg.dist
               }' config/registry.json > /tmp/registry.json
            mv /tmp/registry.json config/registry.json
            
            echo "  ✅ $NAME@$VERSION"
            UPDATED=$((UPDATED + 1))
          done
          
          echo ""
          echo "📊 同步完成: $UPDATED 成功, $FAILED 失败"

      - name: Update registry version
        run: |
          # 更新 registry 的时间戳
          jq --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
             '.updated_at = $ts' config/registry.json > /tmp/registry.json
          mv /tmp/registry.json config/registry.json

      - name: Commit and push
        id: commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add config/registry.json
          
          # 检查是否有变化
          if git diff --cached --quiet; then
            echo "ℹ️ 无变化，跳过提交"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            git commit -m "chore: sync registry $(date -u +%Y-%m-%d)"
            git push
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Update registry release
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "📦 更新 registry release..."
          # 检查 registry release 是否存在
          if gh release view registry &>/dev/null; then
            gh release upload registry config/registry.json --clobber
          else
            gh release create registry \
              --title "Package Registry" \
              --notes "This release contains the package registry for Dec." \
              config/registry.json
          fi
          echo "✅ Registry release 更新完成"
