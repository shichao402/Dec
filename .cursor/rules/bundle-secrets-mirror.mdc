---
description: Dec bundle 与 Bitwarden secrets bundle 同构原则、存储根分离、零重叠 invariant、TUI pull 流程
globs: pkg/app/**/*.go, internal/tui/**/*.go, schema/secrets/**/*.proto, schema/dec/**/*.proto, Documents/**
alwaysApply: false
---

# Bundle ↔ Secrets Bundle 同构

Dec Git Vault 与 Bitwarden 以 **bundle** 为对称组织单位；**project**（`projects/<name>.yaml`）声明启用哪些 bundle。Bitwarden 侧称为 **secrets bundle**。Git Vault 顶层仅含 `projects/` 与 `bundles/`；bundle 资产位于 `bundles/<name>/`。

## 存储根分离（核心）

| 平面 | 存储根 | 内容 |
|------|--------|------|
| Dec（非敏感） | 项目 **`.dec/`** | cache、config 等；渲染到 `.cursor/` 等 IDE 目录 |
| Bitwarden（敏感） | **项目根** 或 **`~/.ssh/`** | Secure Note / mise env 直接落地；SSH Key 落地机器级 `~/.ssh/` |

- bundle 内目录结构 **同构**（`skills/`、`rules/`、`mcp/` 等），但 **存储根完全不同**。
- 默认 **同名绑定**：project 的 `bundles` / 本地 `enabled_bundles` 中的 `vikunja` ↔ Bitwarden folder `vikunja_workflow`（或同名）。
- Bitwarden Secure Note **名称** = 敏感文件在项目根的 **目标相对路径**（如 `.config/mise/conf.d/vikunja.toml`）。
- **SSH Key**：Bitwarden Item Name = 逻辑名，Notes = hosts（一行一个）；Pull → `~/.ssh/dec_<bundle>_<name>` + Dec 管理 `~/.ssh/config` 区块；**无** 项目根 `keys/`。

## 落地前校验（硬约束）

所有校验必须在**写任何文件之前**完成——note 名来自远端，未校验就落盘等于允许远端覆盖项目内任意文件。
落点在 `pkg/secrets/landing.go` 的 `ValidateLandingPaths`。

- **非法路径**：绝对路径、`~` 前缀、`..` 逃逸、盘符 → 拒绝。
- **`.dec/` 重叠**：`.dec/` 树与敏感落地路径**禁止相交**；敏感文件不写入 `.dec/cache/`。
- **跨 folder 撞车**：两个 Bitwarden folder 各有一条 note 指向同一落地路径 → 拒绝。
  必须把所有 folder 的 note **汇总后一次校验**；逐 folder 边取边写看不见撞车。
- **符号链接逃逸**：落地路径经 symlink 指到项目根外 → 拒绝。
- **git 跟踪**：落地路径已被 git 跟踪或未被 `.gitignore` 覆盖 → 拒绝（防把密钥提交进仓库）。

## Push 语义（硬约束）

- 权威索引是**远端 folder 的 note 列表**，不扫本地目录：落地路径散在项目根，没有可靠的本地枚举方式。
- push **绝不删远端 note**。本地缺文件只记进 `MissingLocal` 报告——枚举漏一个就等于静默删掉一条真密钥。
- 删除只走 Delete 页的逐条确认。
- 新增 secret 走 TUI Project 页 `A`（`app.AddProjectSecret`），不由 push 隐式创建。
- pull **不做清理**：落地路径就是消费者路径，没有可以安全整棵删掉的目录。

## MCP 与 mise

- MCP 通过 **mise** 启动（`"command": "mise"`）；env 由 `.config/mise/conf.d/*.toml` 提供。
- **不要** 用 `{{TOKEN}}` / `${VAR}` 占位符替换作为 secrets 注入主路径。

## Pull 流程（TUI-first）

0. 解析 `project_name` → vault `projects/<name>.yaml` → bundles 列表（或本地 `enabled_bundles`）
1. 拉 Dec Git bundle → `.dec/cache/<bundle>/`
2. **自动**从各 folder **取回** Secure Note（先不写盘）
3. 汇总全部落地路径，一次校验（见上节）
4. 落地：Secure Note → **项目根相对路径**；SSH Key → **`~/.ssh/`**
5. 从 cache 渲染安装 IDE + 非敏感 vars 占位符替换

用户在 TUI **Run** 页一次 pull bundle 完成上述步骤；**不要**新增独立的 secrets pull CLI 子命令。

## 参考

- 落地路径决策：`Documents/decisions/0001-secrets-landing-path.md`
- 详细设计：`Documents/BUNDLE-SECRETS-MODEL.md`
- Schema：`schema/dec/v1/projects.proto`（`Project`）、`schema/secrets/v1/config.proto`（`BundleBinding`）、`schema/dec/v1/`（`Bundle`、`enabled_bundles`）

**无本地同步状态文件**：不要重新引入 `state.json` / `SyncStateFile` 之类的本地索引。
本地索引会与远端漂移，而这个索引的错误后果是删错密钥。
