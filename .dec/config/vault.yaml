# Vault 配置
# 声明本项目需要从个人知识仓库同步的资产
# 运行 dec sync 时会自动从 Vault 拉取这里列出的内容

# Skills（Agent 能力包）
vault_skills:
  # - create-api-test

# Rules（Agent 行为规则）
vault_rules:
  - 00-private-overview

# MCPs（外部工具配置）
vault_mcps:
  - dec
