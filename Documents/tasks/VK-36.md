# VK-36 阶段 1：抽离 config init 用例层并引入初版 pkg/app

- Vikunja 项目：`Dec`（ID `7`）
- Vikunja 任务：`36`
- 任务链接：`http://vikunja.firoyang.com/tasks/36`
- 计划目录：`Documents/tasks/`
- 命名约定：`VK-<id>.md`

## 本轮停止条件

- `dec config init` 的仓库扫描、项目配置写入、变量模板生成迁移到 `pkg/app`
- 现有 CLI 行为保持兼容，交互式编辑器仍由 `cmd/config.go` 负责
- 补齐关键路径单元测试
- `Documents/ARCHITECTURE.md` 同步新的模块边界

## 实施步骤

1. 新增 `pkg/app/events.go`，定义初版结构化事件模型与空实现 reporter。
2. 新增 `pkg/app/project.go`，封装项目概览、repo 扫描和 `config init` 准备逻辑。
3. 修改 `cmd/config.go`，改为调用 `pkg/app` 用例层，并保留现有终端输出与编辑器交互。
4. 为 `pkg/app` 和受影响命令补测试，覆盖未连接、保留已有配置、空仓库等关键路径。
5. 更新 `Documents/ARCHITECTURE.md`，记录 `pkg/app` 的职责边界。

## 测试范围

- `go test ./...`

## 涉及模块

- `pkg/app/events.go`
- `pkg/app/project.go`
- `pkg/app/project_test.go`
- `cmd/config.go`
- `Documents/ARCHITECTURE.md`
