---
description: 设岗。把会重复的劳动收成工具+错误码+岗位说明，不要给工具内置聊天 Agent。
---

按 **forge-robot** skill 执行（Skill 与 Command 同名；拉取后 IDE 目录可能带 `dec-` 前缀）。先读 `dec-forge-robot/SKILL.md`，再按清单设岗。

若用户没给全，一次问完这 5 项：岗位短名、日常动作、禁止事项、失败找谁、怎么跑（定时/手动）。

只做一次的活直接干，不要设岗。会重复的，必须交出：可运行的手、错误码、岗位 Skill。禁止在工具进程里塞聊天循环。带着跑通一次再结束。
