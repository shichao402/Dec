---
description: 开发期本机 Console 只有一条生效命令，不要每次换搞法，也不要当成发版
alwaysApply: true
---

# 开发期本机重新部署

改完代码要在**这台开发机**上生效：本地打一份 Console 装上去，覆盖现有安装。
机器上永远只有一份。这是开发循环，**不是**发版：用户面安装包只走 GitHub Actions / RUP
（`dev/v*`、`stable/v*` tag），不要因为本机已经能用就去打 tag 或改渠道。

```bash
python scripts/build-console.py --deploy
```

`--deploy` 会复用 `node_modules`、停掉正在跑的 Console，再静默装上刚打的包
（Windows 是 NSIS，`/S`）。只想拿安装包、自己点安装时用 `--skip-deps`，产物在
`dist/dec-console-<os>-<arch>.<ext>`。增量构建约 3 分钟。

Console 启动后从自身内置 resources 把运行时套件（`dec-server`、`dec-mcp`、`dec-exec`、`dec-host-setup`）释放到 `~/.dec/bin`，**不需要**单独部署运行时：Console 与各组件出自同一次构建，版本天然相等。改前端要热重载时才用 `--prepare-runtime-only` + `cd client && npm run tauri dev`，其版本同样取 `version.json`，与刚装的 Console 一致。

## 禁止

- 把本机覆盖安装说成「已发布」，或用它代替 CI / RUP
- 单独覆盖 `~/.dec/bin`，或引入任何「让运行时对齐已装 Console」的版本探测。**版本源只有 `version.json`**
- 新增 `install-dev.sh`、`redeploy.ps1`、`make install-dev` 等第二入口
- 用 `go build` / `go install` 把任何运行时组件丢进 PATH 或 NSIS 目录当入口
- 为发布慢就去打 tag、跑 GitHub Actions、改 RUP 渠道
- 另开一套安装根；隔离实验才用 `DEC_HOME`，且 Console 与 `dec-mcp` 的 command 必须一起指向该沙箱

## 为什么必须一份

- **目录**：NSIS 装 Console 程序；`~/.dec` 是这台机器上的 Dec（运行时套件、配置、secrets、服务锁）。运行时组件不进 NSIS 目录。
- **版本**：Console 拥有 `~/.dec/bin`，要求每个组件都与 Console 严格相等（ADR 0021）。高于就停服改写，低于就拒绝控制。
- **发现**：Console 读/写 `${DEC_HOME}/run/server.json`（默认 `~/.dec`）并拉起 `DEC_HOME/bin` 里的 `dec-server`；`dec-mcp` 只发现 `${DEC_HOME}/run/console.json`，经唯一 Console 网关转发（ADR 0025）。

两份运行时或两个版本号，必然演化成互相改写与拒绝连接。

装完后 Cursor 里的 `dec-mcp` 仍是旧进程，需重载 MCP 才用上新二进制。
