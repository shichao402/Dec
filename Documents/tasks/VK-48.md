# VK-48 阶段 3B：TUI Settings 页接入仓库连接与全局 IDE 配置

- Vikunja 项目：`Dec`（ID `7`）
- Vikunja 任务：`48`
- 任务链接：`http://vikunja.firoyang.com/tasks/48`
- 计划目录：`Documents/tasks/`
- 命名约定：`VK-<id>.md`

## 本轮停止条件

- `pkg/app` 提供可复用的 Settings 用例层，承接 repo 连接、默认 IDE 保存、内置资产安装与全局 vars 模板准备
- `dec config repo` / `dec config global` CLI 改为复用 app 层，避免和 TUI 再次分叉
- TUI `Settings` 页支持 repo URL 编辑、IDE 勾选与保存，保存后刷新首页 / Assets / Settings 状态
- 补齐 `pkg/app`、`cmd`、`internal/tui` 的关键测试
- `Documents/ARCHITECTURE.md` 同步 Settings 页和全局设置的新边界

## 实施步骤

1. 在 `pkg/app/settings.go` 新增全局设置状态加载、仓库连接、全局 IDE 保存和内置资产安装的用例层。
2. 保留 `cmd/config.go` / `cmd/repo.go` 的终端输出职责，但改为调用 app 层结果。
3. 扩展 `internal/tui/model.go`，让 `Settings` 页可浏览、编辑并保存全局设置。
4. 增加 `pkg/app/settings_test.go`、`cmd/config_test.go`、`internal/tui/model_test.go` 覆盖成功路径和关键交互。
5. 更新 `Documents/ARCHITECTURE.md`，把 Settings 页从占位改为已落地能力。

## 测试范围

- `go test ./...`

## 涉及模块

- `pkg/app/settings.go`
- `pkg/app/settings_test.go`
- `cmd/repo.go`
- `cmd/config.go`
- `cmd/config_test.go`
- `internal/tui/model.go`
- `internal/tui/model_test.go`
- `Documents/ARCHITECTURE.md`
