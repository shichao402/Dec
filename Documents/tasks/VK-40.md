# VK-40 阶段 2：引入 TUI Shell 与默认入口分流

- Vikunja 项目：`Dec`（ID `7`）
- Vikunja 任务：`40`
- 任务链接：`http://vikunja.firoyang.com/tasks/40`
- 计划目录：`Documents/tasks/`
- 命名约定：`VK-<id>.md`

## 本轮停止条件

- `dec` 在交互式无参终端中默认进入最小可用 TUI shell
- `dec pull`、`dec push`、`dec version`、`dec --help` 等现有 CLI 入口保持不变
- 支持 `DEC_NO_TUI=1` 作为隐藏回退开关
- 补齐默认入口分流与最小 TUI shell 的关键测试
- `Documents/ARCHITECTURE.md` 同步 TUI Shell 的落地边界

## 实施步骤

1. 梳理根命令当前入口和 TUI 文档约束，确定默认分流条件与最小 shell 展示内容。
2. 新增 `internal/tui` 基础结构，实现 Bubble Tea 根程序、首页主体、导航、状态栏和日志区的最小版本。
3. 修改 `cmd/root.go` / `main.go`，让无参交互式启动走 TUI，其余显式命令继续走 Cobra。
4. 为入口分流和 TUI 首页状态补测试，覆盖 TTY、非 TTY、`--help`、`DEC_NO_TUI=1` 等关键路径。
5. 更新 `Documents/ARCHITECTURE.md`，记录当前 TUI Shell 边界和默认入口行为。

## 测试范围

- `go test ./...`

## 涉及模块

- `main.go`
- `cmd/root.go`
- `cmd/root_test.go`
- `internal/tui/*`
- `go.mod`
- `Documents/ARCHITECTURE.md`
